<h1>ジャンル一覧・追加</h1>

<%= form_with model: @genre, url: admin_genres_path do |f| %>

<div class="form-group">
  <tbosy>
    <tr>
       <td>
        <label for="genres">ジャンル名</label>
         <br><%= f.text_field :name %>
          </div>
          <div class="form-group">
           <%= f.submit '新規登録' %>
           <% end %>

          </div>
       </td>
    </tr>
  </tbody>

<% @genres.each do |genre| %>
<%= genre.name %>
<%= link_to "編集する", edit_admin_genre_path(genre) %>
<% end %>
<td>

</td>

