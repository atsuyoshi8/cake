<main>
  <p id="notice"></p>
<div class="container px-5 px-sm-0">
  <div class="row">

<div class="col-md-8 offset-md-1">
    <h2 class="mt-3">注文履歴一覧</h2>
    <table class="table table-hover table-inverse">
      <thead>
       <tr>
          <th>購入日時</th>
          <th>注文個数</th>
          <th>注文ステータス</th>
          <th></th>
          <th colspan="3"></th>
        </tr>
       </thead>
      <tbody>
           <% @orders.each do |order| %>
         <tr>
          <td><%=  link_to order.created_at,admin_order_path(order.id) %></td>
          <td><%= order.amount %></td>
          <td><%= order.status %></td>
          <td></td>
         </tr>
           <% end %>
      </tbody>
     </table>
    </div>
   </div>
  </div>
 </miain>

